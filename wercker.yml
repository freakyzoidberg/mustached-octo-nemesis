box: wercker/nodejs
build:
    steps:
      - npm-install:
          strict-ssl: false
      - script:
          name: ensure bindings
          code: npm rebuild bcrypt
      - jshint:
          use_strict: true
          trailing_whitespace: false
      - npm-test
