box: wercker/nodejs
build:
    steps:
      - npm-install@1.0.5:
          strict-ssl: false
      - script:
          name: ensure bindings
          code: npm rebuild bcrypt
      - jshint:
          use_strict: true
          trailing_whitespace: false
      - npm-test
